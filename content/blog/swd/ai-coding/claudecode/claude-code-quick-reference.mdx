---
title: Claude Code 速查手册
date: '2026-02-12'
tags:
  - 软件开发
  - AI 编程
  - ClaudeCode
published: true
brief: >-
  Claude Code 是 Anthropic 官方推出的 AI 编程 CLI 工具，支持终端、VS Code、JetBrains、桌面应用和 Web 多种使用方式。本文全面介绍 Claude Code 的基础用法、核心功能、命令行参数、交互模式、Plan Mode、Skills、Slash Commands、Hooks、MCP 等高级特性，并提供最佳实践和常见问题解答，帮助开发者高效使用 Claude Code 提升编程效率。
---

Claude Code 是 Anthropic 官方推出的 **AI 编程 CLI 工具**，可以在你的终端、IDE、桌面应用和浏览器中使用，支持读取代码库、编辑文件、运行命令，并与开发工具集成。

## 快速开始

### 安装方式

| 方式 | 命令 | 适用平台 |
|------|--------|----------|
| **原生安装（推荐）** | `curl -fsSL https://claude.ai/install.sh \| bash` | macOS, Linux, WSL |
| **Homebrew** | `brew install --cask claude-code` | macOS |
| **WinGet** | `winget install Anthropic.ClaudeCode` | Windows |
| **PowerShell** | `irm https://claude.ai/install.ps1 \| iex` | Windows PowerShell |
| **CMD** | `curl -fsSL https://claude.ai/install.cmd -o install.cmd && install.cmd && del install.cmd` | Windows CMD |

### 首次使用

```bash
# 在任何项目目录启动 Claude Code
claude

# 首次使用会提示登录
```

运行时会在 `~/.claude/` 自动创建以下目录：
| 目录 | 用途 |
|------|------|
| `cache/` | 缓存数据 |
| `plans/` | Plan Mode 生成的计划文件 |
| `tasks/` | 后台任务数据 |
| `transcripts/` | 会话详细记录 |
| `file-history/` | 文件操作历史 |
| `projects/` | 项目元数据 |
| `todos/` | 任务列表 |
| `telemetry/` | 遥测数据 |
| `session-env/` | 会话环境配置 |

### 多平台使用

| 平台 | 特点 | 适用场景 |
|------|------|----------|
| **Terminal CLI** | 功能最全，支持所有命令行参数 | 本地开发、自动化脚本 |
| **VS Code** | 内联 diff、@-mentions、计划审查 | 日常编码、代码审查 |
| **JetBrains** | 交互式 diff 查看、选择上下文共享 | IntelliJ IDEA、PyCharm、WebStorm |
| **Desktop App** | 可视化 diff、多会话并行、云会话 | 非开发者环境、长时间任务 |
| **Web** | 无本地安装、移动端支持、远程会话 | 移动设备、无本地代码库 |

> 💡 **跨平台同步**：所有平台共享同一配置，`CLAUDE.md`、`settings.json`、MCP servers 在所有环境中生效。

## 核心命令

### 基础命令

| 命令 | 描述 | 示例 |
|------|------|------|
| `claude` | 启动交互式 REPL | `claude` |
| `claude "query"` | 带初始提示启动 REPL | `claude "解释这个项目"` |
| `claude -p "query"` | 通过 SDK 查询后退出 | `claude -p "解释这个函数"` |
| `cat file \| claude -p "query"` | 处理管道内容 | `cat logs.txt \| claude -p "分析"` |
| `claude -c` | 继续当前目录最近会话 | `claude -c` |
| `claude -c -p "query"` | 通过 SDK 继续会话 | `claude -c -p "检查类型错误"` |
| `claude -r "<session>" "query"` | 按 ID 或名称恢复会话 | `claude -r "auth-refactor" "完成 PR"` |
| `claude update` | 更新到最新版本 | `claude update` |
| `claude mcp` | 配置 MCP 服务器 | 见 MCP 文档 |

### 会话管理

```bash
# 继续最近的会话
claude -c

# 按会话名恢复
claude -r "auth-refactor"

# 按 PR 恢复会话（需通过 gh pr create 创建）
claude --from-pr 123

# 远程会话（在 claude.ai/code 上创建）
claude --remote "修复登录 bug"

# 将 Web 会话传送到本地终端
claude --teleport

# 分支会话（恢复时创建新会话 ID）
claude --resume abc123 --fork-session
```

## 交互模式

### 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Tab` | 补全文件名、技能名 |
| `Shift+Tab` (按 1 次) | 自动接受模式 |
| `Shift+Tab` (按 2 次) | **Plan Mode** |
| `Shift+Tab` (按 3 次) | 返回正常模式 |
| `Ctrl+C` | 中断当前操作 |
| `Ctrl+D` | 退出会话 |

### 输入模式

| 模式 | 描述 |
|------|------|
| **Normal** | 正常输入模式，支持工具调用 |
| **Auto-Accept** | 自动接受所有工具调用，无需确认 |
| **Plan Mode** | 只读研究和规划模式，不修改代码 |

### 模式对比与选择

| 模式 | 快捷键 | 工具调用 | 效率 | 安全性 | 主要用途 |
|------|---------|----------|------|--------|----------|
| **Normal** | 默认 | 每次都询问 | 中等 | 高 | 日常编码 |
| **Auto-Accept** | Shift+Tab 按一次 | 自动执行，无确认 | 最高 | 低 | 批量操作 |
| **Plan Mode** | Shift+Tab 按两次 | 只读可用 | 较低 | 最高 | 规划审查 |

**模式切换：** 按 `Shift+Tab` 循环切换（Normal → Auto-Accept → Plan Mode → Normal）

**选择建议：**
- 日常编码，不确定影响 → Normal（每步确认）
- 批量重命名/格式化 → Auto-Accept（自动执行）
- 大型重构/架构设计/代码审查 → Plan Mode（先规划）

## TUI 常用命令

启动 `claude` 进入 TUI（Text User Interface）后，可以使用以下内置命令和操作。

> 💡 **提示**：在 TUI 中输入 `/` 后按 Tab 可查看所有可用命令列表。

### 内置 Slash 命令

> ⚠️ **注意**：以下命令为经验证确认存在的常用命令。建议在 TUI 中输入 `/` 后按 Tab 查看完整列表。

| 命令 | 描述 |
|------|------|
| `/help` | 显示所有可用命令和快捷键 |
| `/clear` | 清空当前对话历史 |
| `/exit` | 退出 Claude Code |
| `/compact` | 压缩对话节省上下文 |
| `/config` | 打开配置面板 |
| `/status` | 显示当前会话状态 |
| `/model` | 切换 AI 模型 |
| `/cost` | 显示 Token 使用和费用 |
| `/vim` | 启用/禁用 Vim 输入模式 |

### 会话恢复（TUI 外操作）

**重要**：恢复历史会话不是通过 TUI 内的 slash 命令，而是通过**命令行参数**启动时指定：

```bash
# 方法 1：继续最近的会话
claude --continue
# 或简写
claude -c

# 方法 2：按会话名恢复
claude --resume "会话名"
# 或简写
claude -r
```

使用 `--resume` 启动时会显示**会话选择器**，允许你选择要恢复的历史会话。恢复后会完整保留上下文，包括消息、工具使用和结果。

## TUI 快捷键

| 操作 | 快捷键/命令 | 描述 |
|------|---------------|------|
| **外部编辑器** | `Ctrl+G` 或 `chat:externalEditor` | 在外部编辑器中打开当前输入 |
| **保存输入** | `Ctrl+S` 或 `chat:stash` | 暂存当前提示词供后续使用 |
| **撤销** | `Ctrl+_` 或 `chat:undo` | 撤销上一次操作 |
| **模型选择** | `Cmd+P` / `Meta+P` | 打开模型选择器切换模型 |
| **扩展思考** | `Cmd+T` / `Meta+T` | 切换扩展思考模式 |
| **粘贴图片** | `Ctrl+V` (Windows: `Alt+V`) | 粘贴图片到附件栏 |
| **查看详细日志** | `Ctrl+O` | 切换完整 transcript 模式 |
| **中断操作** | `Ctrl+C` | 中断当前正在进行的操作 |
| **退出会话** | `Ctrl+D` | 退出 Claude Code |

### 完整快捷键参考

在 TUI 中按 `?` 键可查看当前环境的完整快捷键列表。

#### 全局快捷键（Global Context）

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+C` | 中断当前操作 |
| `Ctrl+D` | 退出 Claude Code |
| `Ctrl+T` | 切换任务列表可见性 |
| `Ctrl+O` | 切换详细日志模式 |

#### 聊天上下文快捷键（Chat Context）

| 快捷键 | 功能 |
|--------|------|
| `Enter` | 发送消息 |
| `Esc` | 取消当前输入 |
| `Shift+Tab` | 循环切换权限模式 |
| `Ctrl+G` | 打开外部编辑器 |
| `Ctrl+S` | 暂存当前输入 |
| `Ctrl+_` | 撤销上一次操作 |

#### 确认对话框快捷键（Confirmation Context）

| 快捷键 | 功能 |
|--------|------|
| `Y` 或 `Enter` | 确认操作 |
| `N` 或 `Esc` | 拒绝操作 |
| `↑` / `↓` | 上/下选择选项 |
| `Tab` | 下一个字段 |
| `Shift+Tab` | 上一个字段 |
| `Ctrl+E` | 切换权限说明 |

### 终端复用器冲突

某些快捷键可能与终端复用器冲突：

| 快捷键 | 冲突 | 解决方案 |
|--------|--------|----------|
| `Ctrl+B` | tmux 前缀 | 按两次发送到 tmux |
| `Ctrl+A` | GNU screen 前缀 | 同上 |
| `Ctrl+Z` | Unix 进程挂起 | 避免使用 |

### 自动补全

| 快捷键 | 功能 |
|--------|------|
| `Tab` | 接受建议/补全 |
| `↑` / `↓` | 上/下一条建议 |
| `Esc` | 关闭补全菜单 |

### Diff 查看器

当 Claude 显示代码 diff 时：

| 快捷键 | 功能 |
|--------|------|
| `←` / `→` | 上/下一个 diff 源 |
| `↑` / `↓` | 上/下一个文件 |
| `Enter` | 查看 diff 详情 |
| `Esc` | 关闭 diff 查看器 |

## Plan Mode

### 什么是 Plan Mode

Plan Mode 是 Claude Code 的三种核心模式之一，提供**只读研究和规划环境**。在此模式下，Claude 会生成详细的 Markdown 格式实施计划，保存到 `plans/` 文件夹，供你审批后再执行。

### 激活方式

```bash
# 方式 1：Shift+Tab 按两次
claude
# 然后按 Shift+Tab 两次进入 Plan Mode

# 方式 2：使用 /plan 命令
/plan
帮我规划用户认证系统的实现

# 方式 3：直接请求
进入 plan mode，帮我规划这个功能的实现
```

### Plan Mode 工作流程

```
用户请求
    ↓
进入 Plan Mode
    ↓
Claude 深度分析代码库
    ├── 读取相关文件
    ├── 分析架构
    └── 识别依赖
    ↓
生成详细计划（Markdown）
    └── 保存到 plans/ 目录
    ↓
用户审查计划
    ├── 修改计划（如需要）
    └── 确认或拒绝
    ↓
（确认后）开始实施
```

### 适用场景

- **大型重构前**：规划影响范围、识别风险点
- **新功能开发**：先设计架构再编码
- **架构评审**：不修改代码，仅分析方案
- **团队协作**：将计划共享给团队成员审查

> ⚠️ **已知问题**：Windows 上 Claude Code 2.1.3 版本存在 Shift+Tab 无法进入 Plan Mode 的 bug，仅循环 Auto-Accept 和 Normal 模式（2026 年 1 月报告）。

## 命令行参数

### 常用标志

| 标志 | 描述 | 示例 |
|------|------|------|
| `--model` | 设置会话模型（sonnet/opus/haiku 或全名） | `claude --model sonnet` |
| `--agent` | 指定当前会话使用的 agent | `claude --agent my-agent` |
| `--agents` | 通过 JSON 动态定义子 agent | 见下文 Agents 格式 |
| `--permission-mode` | 以指定权限模式开始 | `claude --permission-mode plan` |
| `--dangerously-skip-permissions` | 跳过所有权限提示（谨慎使用） | `claude --dangerously-skip-permissions` |
| `--tools` | 限制可使用的工具 | `claude --tools "Bash,Edit,Read"` |
| `--allowedTools` | 无需提示即可执行的工具 | `claude --allowedTools "Read" "Bash(git log *)"` |
| `--chrome` | 启用 Chrome 浏览器集成 | `claude --chrome` |
| `--verbose` | 启用详细日志 | `claude --verbose` |
| `--version`, `-v` | 输出版本号 | `claude -v` |

### 工作目录

| 标志 | 描述 | 示例 |
|------|------|------|
| `--add-dir` | 添加额外工作目录 | `claude --add-dir ../apps ../lib` |
| `--setting-sources` | 加载设置的来源（user,project,local） | `claude --setting-sources user,project` |
| `--settings` | 从 JSON 文件或字符串加载设置 | `claude --settings ./settings.json` |

### 输出控制

| 标志 | 描述 | 示例 |
|------|------|------|
| `--print`, `-p` | 打印响应而不进入交互模式 | `claude -p "查询"` |
| `--output-format` | 输出格式（text/json/stream-json） | `claude -p --output-format json "查询"` |
| `--input-format` | 输入格式（text/stream-json） | `claude -p --input-format stream-json "查询"` |
| `--no-session-persistence` | 禁用会话持久化 | `claude -p --no-session-persistence "查询"` |

### Agent 自定义

```bash
claude --agents '{
  "code-reviewer": {
    "description": "专家代码审查员，代码修改后主动使用",
    "prompt": "你是高级代码审查员。关注代码质量、安全性和最佳实践。",
    "tools": ["Read", "Grep", "Glob", "Bash"],
    "model": "sonnet"
  },
  "debugger": {
    "description": "调试专家，处理错误和测试失败",
    "prompt": "你是专家调试员。分析错误、识别根因并提供修复方案。"
  }
}'
```

### 系统提示自定义

| 标志 | 行为 | 适用模式 | 使用场景 |
|------|------|----------|----------|
| `--system-prompt` | **替换**整个默认提示词 | Interactive + Print | 完全控制 Claude 行为 |
| `--system-prompt-file` | 用文件内容**替换** | Print only | 从文件加载可复用提示词 |
| `--append-system-prompt` | **追加**到默认提示词 | Interactive + Print | 添加特定指令，保留默认能力 |
| `--append-system-prompt-file` | 将文件内容**追加**到默认提示词 | Print only | 版本控制的额外指令 |

```bash
# 完全替换
claude --system-prompt "你是 Python 专家，只写带类型注解的代码"

# 追加指令（推荐）
claude --append-system-prompt "总是使用 TypeScript 并包含 JSDoc 注释"

# 从文件加载
claude -p --system-prompt-file ./custom-prompt.txt "查询"
```

## Skills

### 什么是 Skills

Skills 是可复用的**提示词模板**和工具组合，打包成可执行命令。Skills 可以：
- 封装复杂的工作流
- 共享给团队使用
- 从社区下载使用
- 在 `.claude/skills/` 目录管理

### 创建 Skill

在 `.claude/skills/<skill-name>/` 目录创建 `SKILL.md` 文件：

```markdown
---
name: code-review
description: 执行全面的代码审查，包括安全性、性能和最佳实践检查
argument-hint: "[文件路径或目录]"
allowed-tools: Read, Grep, Grep, Bash
---

你是代码审查专家。执行以下步骤：

1. 读取指定文件或目录
2. 分析代码质量
3. 检查安全性问题
4. 评估性能
5. 提供改进建议

使用 Grep 搜索常见反模式，使用 Bash 运行 linting 工具。
```

### 使用 Skill

```bash
# 使用 Slash 命令
/code-review src/components/

# 使用 Tab 补全选择技能
claude
# 然后输入 / + Tab 选择 code-review
```

### Skill Frontmatter 字段

| 字段 | 必需 | 描述 |
|------|--------|------|
| `name` | 是 | Skill 唯一标识符（用于命令） |
| `description` | 是 | Skill 功能描述 |
| `argument-hint` | 否 | 参数提示（如 `[文件路径]`） |
| `allowed-tools` | 否 | 允许使用的工具列表 |
| `max-turns` | 否 | 最大轮次限制 |

### Hooks

Hooks 可以在 Skills 中定义，在组件生命周期中执行：

```markdown
---
name: my-skill
description: 带 Hooks 的 Skill
hooks:
  before-start: "echo 'Skill 开始前执行'"
  after-complete: "echo 'Skill 完成后执行'"
---
```

可用 Hook 类型：
- `before-start`: Skill 开始前
- `after-complete`: Skill 完成后
- `on-error`: 错误发生时

## Slash Commands

### 什么是 Slash Commands

Slash Commands 是自定义命令，用于**编排复杂工作流**，可由 Claude 自动创建或手动定义。

### 手动创建 Slash Command

在 `.claude/commands/<command-name>.md` 文件：

```markdown
---
name: refactor
description: 重构指定文件或目录
---

使用 Plan Mode 分析代码，然后实施重构：
1. 进入 Plan Mode
2. 分析代码结构
3. 提出重构计划
4. 等待用户确认
5. 执行重构
```

### 使用 Slash Command

```bash
claude
/refactor src/utils/

# 或使用 Tab 补全
# 输入 / 然后 Tab 浏览可用命令
```

### 自动创建 Slash Command

```bash
# 直接让 Claude 创建
claude
帮我创建一个 slash command，用于自动测试和部署流程

# Claude 会生成并保存命令文件
```

## MCP (Model Context Protocol)

### 什么是 MCP

MCP（Model Context Protocol）是 Claude Code 的**扩展协议**，允许连接外部工具和服务，如：
- 数据库查询
- API 集成
- 自定义工具
- 第三方服务

### 配置 MCP

```bash
# 基础配置
claude mcp

# 从配置文件加载
claude --mcp-config ./mcp.json

# 严格模式（仅使用指定 MCP 服务器）
claude --strict-mcp-config --mcp-config ./mcp.json
```

### MCP 配置示例

```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/path/to/allowed"]
    },
    "postgres": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres"]
    }
  }
}
```

### MCP 工具使用

配置后，Claude 可以使用 MCP 提供的工具：

```bash
claude
# Claude 会自动发现并使用 MCP 工具
查询数据库中的所有用户

# 使用文件系统 MCP 工具
列出 /var/logs 目录的内容
```

## CLAUDE.md

### 项目级配置

`.claude/CLAUDE.md` 文件存储项目特定配置，会被自动加载到系统提示词中：

```markdown
# 项目配置

## 技术栈
- 前端：React + TypeScript
- 后端：Node.js + Express
- 数据库：PostgreSQL

## 代码风格
- 使用 ESLint 配置
- 组件使用函数式
- Hooks 命名以 use 开头

## 常用命令
- 开发：npm run dev
- 测试：npm test
- 构建：npm run build

## 禁止行为
- 不使用 var，只用 const/let
- 不提交 node_modules
```

### 配置加载优先级

配置来源优先级（从高到低）：
1. `--settings` 命令行参数
2. `--setting-sources` 指定的来源（user, project, local）
3. 项目级 `.claude/settings.json`
4. 用户级 `~/.claude/settings.json`

## Settings

### 配置文件位置

| 级别 | 路径 |
|------|------|
| **用户级** | `~/.claude/settings.json` |
| **项目级** | `.claude/settings.json` |
| **本地级** | 当前目录 `.settings.json` |

### 常用设置

```json
{
  "model": "claude-sonnet-4-5",
  "agent": "default",
  "permissionMode": "auto",
  "autoAcceptMode": false,
  "allowedTools": ["Read", "Write", "Edit", "Bash"],
  "defaultHooks": {
    "before-action": "echo '即将执行操作'",
    "after-action": "echo '操作完成'"
  },
  "mcpServers": {
    "my-server": {
      "command": "npx",
      "args": ["@my/mcp-server"]
    }
  }
}
```

### 权限模式

| 模式 | 描述 |
|------|------|
| `auto` | 自动根据操作危险性提示 |
| `always` | 所有操作都提示 |
| `never` | 从不提示（等同于 `--dangerously-skip-permissions`） |
| `plan` | Plan Mode，只读不写 |

## 最佳实践

### 使用 Plan Mode 处理复杂任务

```bash
claude
# 进入 Plan Mode
 Shift+Tab (两次)

# 规划大型重构
规划将 Class 组件迁移到 Hooks 的完整方案
```

### 创建团队共享 Skills

```bash
# 创建代码审查 Skill
mkdir -p .claude/skills/code-review
cat > .claude/skills/code-review/SKILL.md << 'EOF'
---
name: review
description: 代码审查
allowed-tools: Read, Grep, Bash
---
执行全面的代码审查...
EOF

# 将 .claude 目录加入版本控制
git add .claude/skills/
```

### 使用 Auto-Accept 加快重复操作

```bash
# 进入 Auto-Accept 模式
claude
 Shift+Tab (按一次)

# 批量重命名文件
将所有 .jsx 文件重命名为 .tsx
```

### 配置 MCP 扩展能力

```bash
# 配置数据库 MCP
claude mcp

# 添加文件系统访问
claude --mcp-config '{"mcpServers":{"filesystem":{"command":"npx","args":["-y","@modelcontextprotocol/server-filesystem","."]}}'
```

### 使用项目级 CLAUDE.md

```markdown
# .claude/CLAUDE.md

# 项目：电商后台系统

## 关键信息
- 后端框架：NestJS
- ORM：Prisma
- 认证：JWT

## 测试
- 单元测试：`npm test`
- E2E 测试：`npm run test:e2e`

## 部署
- 预发：deploy staging
- 生产：deploy production
```

## 常见问题

### Q: 如何查看当前会话 ID？

```bash
# 在交互模式中输入
/session

# 会话 ID 保存在 .claude/sessions/ 目录
```

### Q: 如何禁用特定工具？

```bash
claude --tools "Read,Edit,Bash"
# 仅允许使用 Read, Edit, Bash

# 或使用禁止列表
claude --disallowedTools "Write,Delete"
```

### Q: 如何在脚本中使用 Claude Code？

```bash
# Print 模式输出 JSON
claude -p --output-format json "分析这段代码"

# 限制最大预算
claude -p --max-budget-usd 1.00 "查询"

# 限制轮次
claude -p --max-turns 3 "任务"
```

### Q: 如何调试 Claude Code？

```bash
# 启用调试模式
claude --debug "api,mcp"

# 详细日志
claude --verbose

# 查看版本
claude -v
```

### Q: Shift+Tab 无法进入 Plan Mode？

**Windows 已知问题**（Claude Code 2.1.3）：
```bash
# 替代方案：使用 /plan 命令
claude
/plan
帮我规划这个功能
```

## 进阶技巧

### 结合 Git 使用

```bash
# 结合 git 操作
claude
# 在会话中
运行 git status 分析当前状态
根据 git diff 创建提交
```

### 背景任务

```bash
# 启动后台任务（需配合插件）
claude --plugin-dir ./my-plugins

# Task 工具支持后台执行
# 在会话中让 agent 在后台运行长时间任务
```

### IDE 集成

```bash
# 自动连接 IDE
claude --ide

# VS Code 中使用
# 安装扩展后，使用 Cmd+Shift+P 打开命令面板
# 输入 "Claude Code" 选择 "Open in New Tab"
```

### 远程会话

```bash
# 在 claude.ai/code 上创建会话
claude --remote "运行测试套件"

# 传送到本地终端
claude --teleport
```

## 参考链接

- [Claude Code 官方文档](https://code.claude.com/docs/en/overview) - 概览和入门指南
- [CLI 参考文档](https://code.claude.com/docs/en/cli-reference) - 完整命令行参数
- [Skills 文档](https://code.claude.com/docs/en/skills) - Skills 创建和管理
- [Interactive 模式](https://code.claude.com/docs/en/interactive) - 交互功能和快捷键
- [Settings 配置](https://code.claude.com/docs/en/settings) - 配置选项
- [MCP 文档](https://modelcontextprotocol.io/) - Model Context Protocol
- [Claude Code CLI: The Definitive Technical Reference](https://blakecrosley.com/en/guides/claude-code) - 完整技术参考 (Jan 15, 2026)
- [The Complete Guide to Claude Code v2 - Reddit](https://www.reddit.com/r/ClaudeAI/comments/1qcwckg/the_complete_guide_to_claude_code_v2_claudemd_mcp/) - v2 完整指南 (Jan 14, 2026)
- [How to Use Claude Code: Specs, Skills, Commands and Hooks](https://levelup.gitconnected.com/how-to-use-claude-code-bed73d273638) - 技能和钩子指南 (Jan 28, 2026)
- [Complete ClaudeCode Slash Command Reference](https://medium.com/@duke.pearson700/complete-claudecode-slash-command-reference-practical-usage-and-real-world-examples-02f434f73855) - 完整 Slash 命令参考
- [Claude Code CLI Cheat Sheet](https://shipyard.build/blog/claude-code-cheat-sheet/) - CLI 速查表
- [Claude Code Cheat Sheet (Beta) - GitHub](https://github.com/Njengah/claude-code-cheat-sheet) - GitHub 速查表
- [A collection of production-ready slash commands - GitHub](https://github.com/wshobson/commands) - 57 个生产就绪的自定义命令
- [20+ Claude Code Shortcuts YouTube Video](https://www.youtube.com/watch?v=pm9mb9GD3Yc) - 快捷键视频教程
- [DefKey Claude Code Shortcuts](https://defkey.com/claude-code-cli-shortcuts) - 平台特定快捷键参考
- [Chinese Tutorial - Claude Code Tips](https://claudecode.tangshuang.net/course/9.1%2520%25E9%2594%25AE%25E7%259B%2598%25E5%25BF%25AB%25E6%258D%25B7%25E9%2594%25AE) - 中文快捷键教程 (40+ 个提示)
